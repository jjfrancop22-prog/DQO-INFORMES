import {eventBus} from '../core/event-bus.js';
import {repositories} from '../data/repositories.js';
const KEY='PEP_INTELLIGENT_NOTIFICATIONS_A7037',READ_KEY='PEP_INTELLIGENT_NOTIFICATIONS_READ_A7037',MAX=220;
const clean=v=>String(v??'').trim(),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const fmtDate=v=>{if(!v)return '—';const d=new Date(`${String(v).slice(0,10)}T12:00:00`);return Number.isNaN(d.getTime())?String(v):d.toLocaleDateString('es-EC')};
const uid=()=>`N_${Date.now()}_${Math.random().toString(36).slice(2,8)}`;
class IntelligentNotificationCenter{
 constructor(){this.items=[];this.read=new Set();this.started=false;this.remoteSeen=new Set();this.filter='ALL'}
 load(){try{this.items=JSON.parse(localStorage.getItem(KEY)||localStorage.getItem('PEP_INTELLIGENT_NOTIFICATIONS_A7033')||'[]');if(!Array.isArray(this.items))this.items=[]}catch{this.items=[]}try{this.read=new Set(JSON.parse(localStorage.getItem(READ_KEY)||localStorage.getItem('PEP_INTELLIGENT_NOTIFICATIONS_READ_A7033')||'[]'))}catch{this.read=new Set()}}
 save(){localStorage.setItem(KEY,JSON.stringify(this.items.slice(0,MAX)));localStorage.setItem(READ_KEY,JSON.stringify([...this.read].slice(-600)))}
 actor(v=''){return clean(v)||'Usuario PEP'} async sample(i){return i?repositories.samples.get(i).catch(()=>null):null}
 signature(n){return [n.type,n.sampleId,n.code,n.date,n.actor,n.source].join('|')}
 classify(n){if(n.kind)return n.kind;if(n.source==='PLANIFICADOR')return n.priority==='high'?'warning':'upcoming';if(n.priority==='high'||['STOPPED','STOPPED_REMOTE','WAITING'].includes(n.type))return'warning';return'info'}
 push(n,{popup=true}={}){const now=Date.now(),item={id:uid(),at:new Date().toISOString(),priority:'normal',category:'MUESTRAS',source:'LOCAL',...n};item.kind=this.classify(item);const sig=this.signature(item);if(this.items.find(x=>this.signature(x)===sig&&now-new Date(x.at).getTime()<4000))return;this.items.unshift(item);this.items=this.items.slice(0,MAX);this.save();this.render();if(popup)this.popup(item)}
 popup(n){const host=document.getElementById('pepSmartPopupHost');if(!host)return;const el=document.createElement('div');el.className=`pep-smart-popup ${n.priority==='high'?'high':''}`;el.innerHTML=`<div class="pep-smart-popup-icon">${n.icon||'🔔'}</div><div><b>${esc(n.title)}</b><p>${esc(n.message)}</p><small>${esc(n.context||'')}</small></div><button type="button">×</button>`;el.querySelector('button').onclick=()=>el.remove();host.prepend(el);setTimeout(()=>el.remove(),6500)}
 unread(){return this.items.filter(x=>!this.read.has(x.id)).length}
 render(){const count=document.getElementById('pepNotificationCount'),list=document.getElementById('pepNotificationList');if(count){const n=this.unread();count.textContent=n>99?'99+':String(n);count.hidden=!n}if(!list)return;const counts={info:0,warning:0,upcoming:0};this.items.forEach(x=>counts[this.classify(x)]++);document.querySelectorAll('[data-pep-notify-filter]').forEach(b=>{const k=b.dataset.pepNotifyFilter;b.classList.toggle('active',k===this.filter);const c=b.querySelector('em');if(c)c.textContent=k==='ALL'?this.items.length:(counts[k]||0)});const visible=this.filter==='ALL'?this.items:this.items.filter(x=>this.classify(x)===this.filter);if(!visible.length){list.innerHTML='<div class="pep-notification-empty">Sin novedades en esta categoría.</div>';return}const groups=[];for(const n of visible){const day=new Date(n.at).toLocaleDateString('es-EC');let g=groups.at(-1);if(!g||g.day!==day){g={day,items:[]};groups.push(g)}g.items.push(n)}list.innerHTML=groups.slice(0,7).map(g=>`<div class="pep-notification-day"><div class="pep-notification-day-title">${esc(g.day)}</div>${g.items.map(n=>`<button class="pep-notification-item ${this.read.has(n.id)?'read':'unread'} kind-${this.classify(n)}" data-notification-id="${n.id}" type="button"><span class="pep-notification-icon">${n.icon||'🔔'}</span><span><b>${esc(n.title)}</b><span>${esc(n.message)}</span><small>${esc(n.context||'')}${n.actor?' · '+esc(n.actor):''} · ${new Date(n.at).toLocaleTimeString('es-EC',{hour:'2-digit',minute:'2-digit'})}</small></span></button>`).join('')}</div>`).join('')}
 mark(v){this.read.add(v);this.save();this.render()} markAll(){this.items.forEach(x=>this.read.add(x.id));this.save();this.render()} clear(){this.items=[];this.read.clear();this.save();this.render()} toggle(force){const p=document.getElementById('pepNotificationPanel');if(!p)return;const o=force??!p.classList.contains('show');p.classList.toggle('show',o);if(o)this.render()}
 context(s){return s?[s.clientId,s.branch,s.matrixId].filter(Boolean).join(' · '):''}
 async localRegistration(d){const s=await this.sample(d.id);if(!s)return;window.__pepNotificationSnapshot?.set(s.id,structuredClone(s));this.push({type:'REGISTERED',sampleId:s.id,code:s.code,title:`Código ${s.code} registrado`,message:`Nueva muestra ${s.codeFull||s.code} · fecha ${fmtDate(s.samplingDate)}.`,context:this.context(s),actor:this.actor(s.updatedBy),icon:'🆕',kind:'info'})}
 async transition(d){const s=await this.sample(d.id);if(!s)return;window.__pepNotificationSnapshot?.set(s.id,structuredClone(s));const to=clean(d.to),action=clean(d.action);if(to==='SAMPLE_REGISTRY')this.push({type:'TO_LAB',sampleId:s.id,code:s.code,title:`${s.code} listo para Laboratorio`,message:'El control previo terminó y la muestra quedó habilitada para ingreso a Laboratorio.',context:this.context(s),actor:this.actor(s.updatedBy),icon:'🧪',kind:'info'});else if(to==='WAITING')this.push({type:'WAITING',sampleId:s.id,code:s.code,title:`${s.code} enviado a En Espera`,message:'La muestra requiere resolución antes de continuar el flujo.',context:this.context(s),actor:this.actor(s.updatedBy),icon:'⏳',kind:'warning'});else if(action.includes('STOP'))this.push({type:'STOPPED',sampleId:s.id,code:s.code,title:`${s.code} DETENIDA`,message:'La muestra fue detenida y requiere seguimiento.',context:this.context(s),actor:this.actor(s.updatedBy),icon:'🛑',priority:'high',kind:'warning'})}
 async reception(d){for(const sid of d.ids||[]){const s=await this.sample(sid);if(s){window.__pepNotificationSnapshot?.set(s.id,structuredClone(s));this.push({type:'RECEPTION',sampleId:s.id,code:s.code,date:d.receivedDate,title:`Recepción asignada · ${s.code}`,message:`Fecha de recepción: ${fmtDate(d.receivedDate)}.`,context:this.context(s),actor:this.actor(d.userId||s.updatedBy),icon:'📅',kind:'info'})}}}
 async lab(d){if(d.bulk)return;const s=await this.sample(d.sampleId),e=await repositories.laboratory.get(d.entryId).catch(()=>null);if(!s)return;this.push({type:'LAB_OFFICIAL',sampleId:s.id,code:s.code,date:e?.receptionDate,title:`${s.code} ingresó a Laboratorio`,message:`Ingreso oficial confirmado${e?.analyst?` · Analista: ${e.analyst}`:''}${e?.maxReportDate?` · Máx. informe: ${fmtDate(e.maxReportDate)}`:''}.`,context:this.context(s),actor:this.actor(e?.updatedBy||e?.officialEntryBy),icon:'🔬',kind:'info'})}
 async remote(d){const domain=clean(d.domain).toUpperCase(),e=d.entity;if(!e||e.deleted)return;const key=`${domain}|${e.id}|${e.revision}`;if(this.remoteSeen.has(key))return;this.remoteSeen.add(key);if(domain==='SAMPLES'){const old=window.__pepNotificationSnapshot?.get(e.id)||null;window.__pepNotificationSnapshot=window.__pepNotificationSnapshot||new Map();window.__pepNotificationSnapshot.set(e.id,structuredClone(e));if(!old){this.push({type:'REGISTERED_REMOTE',sampleId:e.id,code:e.code,title:`Nuevo código ${e.code}`,message:`Registrado desde otro equipo · fecha ${fmtDate(e.samplingDate)}.`,context:this.context(e),actor:this.actor(e.updatedBy),icon:'🆕',source:'OTRO EQUIPO',kind:'info'});return}if(clean(old.receivedDate)!==clean(e.receivedDate)&&e.receivedDate)this.push({type:'RECEPTION_REMOTE',sampleId:e.id,code:e.code,date:e.receivedDate,title:`Recepción actualizada · ${e.code}`,message:`Nueva fecha de recepción: ${fmtDate(e.receivedDate)}.`,context:this.context(e),actor:this.actor(e.updatedBy),icon:'📅',source:'OTRO EQUIPO',kind:'info'});const a=old.workflow?.workflowStage,b=e.workflow?.workflowStage;if(a!==b&&b==='SAMPLE_REGISTRY')this.push({type:'TO_LAB_REMOTE',sampleId:e.id,code:e.code,title:`${e.code} listo para Laboratorio`,message:'Actualización recibida desde otro equipo: muestra habilitada para Laboratorio.',context:this.context(e),actor:this.actor(e.updatedBy),icon:'🧪',source:'OTRO EQUIPO',kind:'info'});if(old.workflow?.decisionStatus!==e.workflow?.decisionStatus&&e.workflow?.decisionStatus==='DETENIDA')this.push({type:'STOPPED_REMOTE',sampleId:e.id,code:e.code,title:`${e.code} DETENIDA`,message:'Cambio recibido desde otro equipo.',context:this.context(e),actor:this.actor(e.updatedBy),icon:'🛑',priority:'high',source:'OTRO EQUIPO',kind:'warning'})}else if(domain==='LABORATORY'&&e.officialEntryAt)this.push({type:'LAB_REMOTE',sampleId:e.sampleId,code:e.code,title:`${e.code} ingresó a Laboratorio`,message:`Ingreso oficial desde otro equipo${e.analyst?` · Analista: ${e.analyst}`:''}.`,context:[e.clientId,e.branch,e.matrixId].filter(Boolean).join(' · '),actor:this.actor(e.updatedBy||e.officialEntryBy),icon:'🔬',source:'OTRO EQUIPO',kind:'info'})}
 syncPlanningAlerts(alerts=[]){const planning=(Array.isArray(alerts)?alerts:[]).slice(0,80).map(a=>({id:`PLAN|${a.clientId}|${a.planId}|${a.year}|${a.label}|${a.title}`,at:new Date().toISOString(),type:'PLANNING_ALERT',priority:a.level==='critical'?'high':'normal',category:'PLANIFICACION',source:'PLANIFICADOR',kind:a.kind||(a.level==='info'?'upcoming':'warning'),title:a.title||'Alerta de monitoreo',message:a.detail||'Revisar obligación de monitoreo.',context:[a.clientName,a.branch,a.matrix,a.label].filter(Boolean).join(' · '),actor:'Planificador inteligente',icon:a.level==='critical'?'🔴':a.level==='info'?'📌':'🟠'}));this.items=[...planning,...this.items.filter(x=>x.source!=='PLANIFICADOR')].slice(0,MAX);this.save();this.render()}
 seedSnapshot(){window.__pepNotificationSnapshot=new Map((window.__pepNotificationRows||[]).map(x=>[x.id,structuredClone(x)]))}
 init(){if(this.started)return;this.started=true;this.load();this.render();document.getElementById('pepNotificationBell')?.addEventListener('click',e=>{e.stopPropagation();this.toggle()});document.getElementById('pepNotificationMarkAll')?.addEventListener('click',()=>this.markAll());document.getElementById('pepNotificationClear')?.addEventListener('click',()=>this.clear());document.getElementById('pepNotificationFilters')?.addEventListener('click',e=>{const b=e.target.closest('[data-pep-notify-filter]');if(b){this.filter=b.dataset.pepNotifyFilter;this.render()}});document.getElementById('pepNotificationList')?.addEventListener('click',e=>{const b=e.target.closest('[data-notification-id]');if(b)this.mark(b.dataset.notificationId)});document.addEventListener('click',e=>{if(!e.target.closest('#pepNotificationPanel')&&!e.target.closest('#pepNotificationBell'))this.toggle(false)});eventBus.on('sample.registration.completed',e=>this.localRegistration(e.detail));eventBus.on('workflow.transition',e=>this.transition(e.detail));eventBus.on('samples.reception-date.assigned',e=>this.reception(e.detail));eventBus.on('laboratory.official-entry.completed',e=>this.lab(e.detail));eventBus.on('sync:remote-applied',e=>this.remote(e.detail));window.addEventListener('pep:planning-alerts',e=>this.syncPlanningAlerts(e.detail?.alerts||[]))}
}
export const intelligentNotificationCenter=new IntelligentNotificationCenter();
